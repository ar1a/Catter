type User {
  id: ID! @id
  username: String! @unique
  password: String!
  meows: [Meow!]!
}

type Meow {
  id: ID! @id
  content: String!
  author: User! @relation(link: INLINE)
  createdAt: DateTime! @createdAt
}
